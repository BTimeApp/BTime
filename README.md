# BTime
BTime is a [Next.js](https://nextjs.org) project.

## Development
To run the development server:

```bash
npm run dev
```
Then open [http://localhost:3000](http://localhost:3000). Others on your local network can also use your IP with the 3000 port to access the site if you've configured your firewall correctly.

